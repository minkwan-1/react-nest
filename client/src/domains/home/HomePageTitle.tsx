import { Typography } from "@mui/material";

const HomePageTitle = () => {
  return (
    <Typography
      sx={{
        fontSize: "32px",
        fontWeight: "bold",
        marginBottom: 3,
      }}
    >
      질문 및 답변
    </Typography>
  );
};

export default HomePageTitle;
